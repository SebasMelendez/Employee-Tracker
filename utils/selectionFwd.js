function decode(selection){

    const choices = ["View All Employees", "Add Employee", "Update Employee Role", "View All roles", "Add Roles","View All Departments", "Add Department"]
    let fwdResult
    let url ='/api/'

    choices.forEach(element => {if(selection){

    }})

    switch(selection){
        case "View All Employees":
            url =+ "departments"
        break
    }


}